    <!--Navigaton-->
    <% if (user && user.isLoggedIn && user.level === 'M') { %>
    <!-- manager Navigation -->
    <nav class="navbar">
        <div class="navbar-container">
            <a href="/" class="navbar-brand">
                <img src="/Ella Rises Logo (3).png" alt="Ella Rises Logo">
            </a>
            <button class="navbar-toggle" id="navbarToggle" aria-label="Toggle navigation menu">
                <span id="menuIcon">☰</span>
            </button>
            <ul class="navbar-menu" id="navMenu">
                <li><a href="/">Home</a></li>
                <li><a href="/mUser">Manage Users</a></li>
                <li class="dropdown" id="metricsDropdown">
                    <span class="dropdown-trigger" onclick="toggleDropdown('metricsDropdown')">Metrics</span>
                    <ul class="dropdown-menu">
                        <li><a href="/mParticipant">Participants</a></li>
                        <li><a href="/mEvent">Events</a></li>
                        <li><a href="/mDonation">Donations</a></li>
                        <li><a href="/mMilestone">Milestones</a></li>
                        <li><a href="/mSurvey">Post Surveys</a></li>
                        <li><a href="/tableau">Dashboard</a></li>
                    </ul>
                </li>
                <li><a href="/visitorDonation">Donate</a></li>
            </ul>
            <div class="navbar-user">
                <a href="/logout" class="navbar-logout">Logout</a>
            </div>
        </div>
    </nav>
    <% } else if (user && user.isLoggedIn && user.level === 'U') { %>
    <!-- user Navigation -->
    <nav class="navbar">
        <div class="navbar-container">
            <a href="/" class="navbar-brand">
                <img src="/Ella Rises Logo (3).png" alt="Ella Rises Logo">
            </a>
            <button class="navbar-toggle" id="navbarToggle" aria-label="Toggle navigation menu">
                <span id="menuIcon">☰</span>
            </button>
            <ul class="navbar-menu" id="navMenu">
                <li><a href="/">Home</a></li>
                <li class="dropdown" id="metricsDropdown">
                    <span class="dropdown-trigger" onclick="toggleDropdown('metricsDropdown')">Metrics</span>
                    <ul class="dropdown-menu">
                        <li><a href="/uParticipant">Participants</a></li>
                        <li><a href="/uEvent">Events</a></li>
                        <li><a href="/uDonation">Donations</a></li>
                        <li><a href="/uMilestone">Milestones</a></li>
                        <li><a href="/uSurvey">Post Surveys</a></li>
                        <li><a href="/tableau">Dashboard</a></li>
                    </ul>
                </li>
                <li><a href="/visitorDonation">Donate</a></li>
            </ul>
            <div class="navbar-user">
                <a href="/logout" class="navbar-logout">Logout</a>
            </div>
        </div>
    </nav>
    <% } else { %>
    <!-- universal Navigation (for visitors/not logged in) -->
    <nav class="navbar">
        <div class="navbar-container">
            <a href="/" class="navbar-brand">
                <img src="/Ella Rises Logo (3).png" alt="Ella Rises Logo">
            </a>
            <button class="navbar-toggle" id="navbarToggle" aria-label="Toggle navigation menu">
                <span id="menuIcon">☰</span>
            </button>
            <ul class="navbar-menu" id="navMenu">
                <li><a href="/">Home</a></li>
                <li><a href="/visitorDonation">Donate</a></li>
                <li><a href="/login">Login</a></li>
            </ul>
        </div>
    </nav>
    <% } %>

    <script>
        // Mobile menu toggle functionality
        document.addEventListener('DOMContentLoaded', function() {
            const navbarToggle = document.getElementById('navbarToggle');
            const navMenu = document.getElementById('navMenu');
            const menuIcon = document.getElementById('menuIcon');
            
            if (navbarToggle && navMenu) {
                navbarToggle.addEventListener('click', function() {
                    navMenu.classList.toggle('active');
                    if (menuIcon) {
                        menuIcon.textContent = navMenu.classList.contains('active') ? '✕' : '☰';
                    }
                });
                
                // Close menu when clicking outside
                document.addEventListener('click', function(event) {
                    if (!navbarToggle.contains(event.target) && !navMenu.contains(event.target)) {
                        navMenu.classList.remove('active');
                        if (menuIcon) {
                            menuIcon.textContent = '☰';
                        }
                    }
                });
                
                // Close menu when clicking on a link
                const navLinks = navMenu.querySelectorAll('a');
                navLinks.forEach(link => {
                    link.addEventListener('click', function() {
                        navMenu.classList.remove('active');
                        if (menuIcon) {
                            menuIcon.textContent = '☰';
                        }
                    });
                });
            }
        });
        
        // Dropdown toggle for mobile
        function toggleDropdown(dropdownId) {
            if (window.innerWidth <= 768) {
                const dropdown = document.getElementById(dropdownId);
                if (dropdown) {
                    dropdown.classList.toggle('active');
                }
            }
        }
    </script>

     